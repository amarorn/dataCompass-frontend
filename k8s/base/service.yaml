apiVersion: v1
kind: Service
metadata:
  name: datacompass-dashboard-service
  labels:
    app: datacompass-dashboard
    component: frontend
    tier: presentation
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: datacompass-dashboard
    component: frontend

